# Blast-radius surfaces: owner/orchestrator review required
# (rationale: plans/archive/rebuild/03-repo-structure.md §5, archived — the live repo
#  layout is the current authority)
/db/migrations/            @EshMis
/packages/py/contracts/    @EshMis
/packages/py/sandbox/      @EshMis
/.github/workflows/        @EshMis
/apps/web/middleware.ts    @EshMis
# The two lists the middleware decides from. Owned for the same reason the
# middleware is: LOCALE_ROUTES entries are rewritten BEFORE the auth gate and
# are therefore public by construction, and a segment added to or removed from
# ROUTED_SEGMENTS changes whether an unknown URL is gated or falls through to a
# 404. Both were reachable without a blast-radius declaration until now.
/apps/web/lib/routed-paths.ts  @EshMis
/services/api/src/majorana_api/auth/  @EshMis
