# Newsroom implementation handoff — 2026-09-14

Publication of the code review, authorized by the owner: branch pushed and PR https://github.com/Leona-Quantum/leona/pull/883 opened against dev. EshMis is assigned and requested as reviewer. The PR body explicitly requests initial Vercel, Secret Manager/Worker and Cloudflare setup, with completion checks. GitHub reports the repository has moved to Leona-Quantum/leona. CI was running at PR creation; no merge or production setup was performed. The implementation-session notes below predate the push.

PR branch: `feature/leona-news-integration`, based on fetched `origin/dev` at `fc4dfe0e`. Prepared in `/Users/suzukirui/Downloads/leona-news-integration`. Original working changes remain in `feature/leona-news-app`; do not submit that branch because it also contains an earlier unrelated website commit. The owner requested a serious implementation of news discovery, Japanese article generation and image/article persistence, with repository analysis before integration.

Implemented: migration 0061 (batches/articles/assets, RLS, composite workspace FKs); scoped API repository; owner/admin editor API and published-only anonymous API; real OpenAI Responses/Web Search/structured output and image generation adapter; durable, fenced checkpoint/reservation pipeline; arXiv supplement; scheduled collection and opt-in automatic publication; retries/dead-letter state; revision/re-verification, image permission metadata and replacement, publish/withdraw, stable correction history; independent public renderer and loopback editorial UI; Dockerfile and runbook. Follow-up adds a Vercel Build Output API package, a separate Vercel project configuration, CI database tests, and reviewed deployment settings in infra/news.json integrated into existing API/Worker rollout with a dark-revision publication-boundary smoke check.

Architecture: only existing API/Worker talk to Postgres. Existing WorkOS scope and newsroom workspace pin protect mutations. Existing queue leases are propagated only to news handlers and checked transactionally before reservations and writes. Scheduler errors are isolated so unrelated jobs continue. No public admin credential is introduced. Generated content and citation presence are screened; human verification is still required before enabling automatic publication. Original image rights are editor-attested, never inferred by AI.

Validation: 126 targeted Python tests and 5 Node tests passed. The PostgreSQL 17 cluster at /tmp/leona-news-pg:55439 was created exclusively for this session; migrations ran up→down→up. Real DB/API/renderer browser QA passed at widths 1440/390/320 with deterministic fixture content and images, plus citation links and editorial review. OpenAI SDK request/response shapes were tested through HTTP mock transport, not paid calls. Ruff, syntax checks, raw-query boundary check, workspace inventory, lock consistency and diff whitespace checks passed. Existing local test dependencies nbformat/defusedxml were installed to make existing API/Worker imports testable; no lock version bumps were made.

No production DB, DNS, deployment, push, public release, real API spend or secret edits occurred. The user's apps/news/.env is retained and ignored. Container build could not run because the Docker daemon is unavailable. API project/model access, actual generated article quality, production backup restore, production migrations, deployment/TLS/DNS and operational alerts remain release checks. Generic inbound RSS ingestion and outbound email/Slack notifications are not implemented; the public RSS output feed is implemented.

Authoritative bootstrap and fallback ops files under ~/Developer were not readable. The required handoff.py command was attempted and could not open the script. Shared STATUS/NEXT/DECISIONS and the continuation template could therefore not be read or updated. This local note records the work and does not replace authority at ~/Developer/ai-ops/desk/leona/plans/.

Next pickup: read apps/news/RUNBOOK.md and PR.md on feature/leona-news-integration. Initial Vercel project connection, Secret Manager setup and Cloudflare news CNAME are still owner setup tasks. Set up the newsroom workspace and existing API/Worker environments, confirm a bounded live API test budget, and run real draft-generation acceptance tests before any publication. Follow owner release approval for the migration, deployment and DNS switch; confirm old news URL retention before changing an existing hostname. Do not mistake test-fixture screenshots for generated or published news.
