id: bench-01
category: "A — Bell/GHZ state prep"
prompt: "Prepare a Bell state on two qubits and measure both."
framework: qiskit
expect:
  verifier_decision: pass
  export_status: lossless
  output_keys: ["counts"]
  saves_artifact: true
