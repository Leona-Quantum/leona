id: bench-28
category: "G — Transpilation/export honesty"
prompt: "Take a circuit that mid-circuit measures a qubit and conditionally applies X, and export it to OpenQASM 3. Classify the export honestly."
framework: qiskit
expect:
  # OpenQASM 3 natively represents mid-circuit measurement and classical control.
  # The artifact must remain lossless instead of inheriting a private-model limit.
  export_status: lossless
  saves_artifact: true
  terminal_reason: ai_review_aligned
