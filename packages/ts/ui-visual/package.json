{
  "name": "@majorana/ui-visual",
  "version": "0.0.0",
  "private": true,
  "description": "Visual/a11y test harness: renders the real @majorana/ui components to static HTML (esbuild + renderToStaticMarkup, tokens/styles inlined from source) and asserts zero WCAG violations via @axe-core/playwright. No Next, no auth. Screenshot source for the later visual-diff slice. Spec: docs/ui/screens-acceptance.md §3.",
  "type": "module",
  "scripts": {
    "render": "node scripts/render.mjs",
    "a11y": "node scripts/render.mjs && playwright test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@majorana/ui": "workspace:*"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.51.1",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "esbuild": "^0.25.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^7.0.2"
  }
}
